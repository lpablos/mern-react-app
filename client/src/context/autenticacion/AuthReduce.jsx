import {
    REGISTRO_EXITOSO,
    REGISTRO_ERROR,
    OBTENER_USUARIO,
    LOGIN_EXITOSO,
    LOGIN_ERROR,
    CERRAR_SESSION,
} from '../../types'

export default (state, action) => {
    switch (action.type) {
        case REGISTRO_EXITOSO:
            break;
        case REGISTRO_ERROR:
            break;
        case OBTENER_USUARIO:
            break;
        case LOGIN_EXITOSO:
            break;
        case LOGIN_ERROR:
            break;
        case CERRAR_SESSION:
            break;
        default:
            break;
    }

}