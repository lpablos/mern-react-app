import React from 'react'
import { Router, Redirect } from 'react-router-dom'
import AuthContext from '../../context/autenticacion/AuthContext'

const RutaPrivada = ({ component: Component}) => {
    return(
        <h1>Hola</h1>
    )
}

export default RutaPrivada